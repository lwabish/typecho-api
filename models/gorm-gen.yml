version: "0.1"
database:
  db: mysql
  dsn: root:root@tcp(127.0.0.1:3307)/typecho?charset=utf8mb4&parseTime=True&loc=Local
  tables:
    - typecho_contents
  outPath: models
  modelPkgName: models
  onlyModel: true
